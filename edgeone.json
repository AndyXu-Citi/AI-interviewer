{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "cloudFunctions": {
    "nodejs": {
      "includeFiles": ["data/**"]
    }
  },
  "agents": {
    "timeout": 300,
    "framework": "openai-agents-sdk",
    "sandbox": {
      "timeout": 300
    }
  }
}
